const generateToken = require("./generate-token")
const add = require("./calculator").add;

//J'ai laissé ce fichie même s'il est inutile dans le contexte
// Suffit de lancer mocha...


//console.log(add(5,6))
//generateToken();
